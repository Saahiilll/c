//write a program to print the address of a variable.use this address to get the value of this variable

#include<stdio.h>
int main(int argc, char const *argv[])
{
    int a;
    printf("the address of the a is %u",&a);
    return 0;
}
